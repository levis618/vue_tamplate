import ajax from './ajax'
const BASE_URL = '/api'

// export const reqLogin = (name, pwd) => ajax.post(`${BASE_URL}/login`, { name, pwd })
// export const reqLoginOut = () => ajax.get(`${BASE_URL}/logout`)
